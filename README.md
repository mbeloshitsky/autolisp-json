JSON parser and formatter written in AutoLISP
=============================================

json.lsp file contains two useful routines, which allows you to interop your autocad data with every application, which supports JSON.
